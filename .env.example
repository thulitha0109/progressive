# Database Configuration (Docker Compose)
POSTGRES_USER="postgres"
POSTGRES_PASSWORD="secure_password_here"
POSTGRES_DB="progressive"

# Database Connection
# Note: In docker-compose, this is constructed automatically.
# For local development (outside Docker), use the exposed port 5433:
DATABASE_URL="postgresql://postgres:secure_password_here@localhost:5433/progressive"

# NextAuth Configuration
# Generate a secret with: npx auth secret or openssl rand -base64 32
AUTH_SECRET="changeme_to_a_long_random_string"

# For Production (Docker): http://your-domain.com
# For Local Dev: http://localhost:3000
AUTH_URL="http://localhost:3000"

# Trust Host (Required for Docker/reverse proxies)
AUTH_TRUST_HOST="true"
